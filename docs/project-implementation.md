# Project Implementation

## Step 1: Project Setup

* Created a project folder in Visual Studio Code.
* Initialized a local Git repository using `git init`.
* Created the required project structure.

## Step 2: Version Control

* Added all project files using `git add .`
* Created commits after every major change.
* Connected the local repository to GitHub.
* Pushed the project to GitHub using `git push`.

## Step 3: Python Development

* Created `main.py` as the entry point.
* Created `scanner.py` to perform network scanning.
* Installed the required Python library (`python-nmap`).
* Installed Nmap on Windows.

## Step 4: Testing

* Executed the application from the VS Code terminal.
* Entered the target IP address.
* Verified that the scan displayed the host status and open ports.

## Step 5: Documentation

* Added README.md
* Added commands.md
* Added report.md
* Added findings.md
* Added screenshots and implementation details.
